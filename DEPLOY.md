# Деплой бота на VDSina (24/7)

Цель: бот крутится круглосуточно сам, переживает сбои и перезагрузки. Дашборд смотрим безопасно через SSH-туннель (порт наружу НЕ открываем).

> ⚠️ ВАЖНО: на одном кошельке должен работать ТОЛЬКО ОДИН бот.
> Перед запуском на сервере **останови локального бота на ПК** (Ctrl+C), иначе два инстанса будут драться за nonce и слать двойные ордера.

---

## 1. Создать сервер
1. Регистрируешься на **vdsina.com**, пополняешь баланс (рубли ИЛИ крипта — в кабинете).
2. Создаёшь VPS: **Ubuntu 24.04**, минимальный тариф (**1 CPU / 1 ГБ ОЗУ / 20 ГБ** — боту хватит с запасом, ~₽330/мес). Локация — **Европа** (Германия/Нидерланды/Финляндия).
3. Получаешь **IP-адрес** и **root-пароль** (придут в кабинете/на почту).

## 2. Подключиться к серверу
С твоего ПК (Windows) открой **PowerShell** и:
```
ssh root@ТВОЙ_IP
```
(первый раз спросит "yes", потом введёшь пароль — он не отображается при вводе, это норм).

## 3. Установить Node.js
На сервере вставь построчно:
```
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs
node -v
```
Должна показаться версия v20.x.

## 4. Залить бота на сервер
Самый простой путь — **WinSCP** (бесплатная программа с окошком, перетаскиваешь мышкой):
1. Скачай WinSCP, подключись: хост = твой IP, юзер = root, пароль = root-пароль.
2. Перетащи папку **`dreamdex-bot`** целиком в `/root/` на сервере.
3. **НЕ копируй папку `node_modules`** (она тяжёлая, поставим на сервере заново) и **НЕ копируй `.env`** (создадим вручную на сервере — так безопаснее).

## 5. Установить зависимости
На сервере:
```
cd /root/dreamdex-bot
npm install
```

## 6. Создать .env с ключом (это делаешь ТЫ, ключ остаётся только на сервере)
```
nano .env
```
Вставь (замени значения на свои):
```
DREAMDEX_PRIVATE_KEY=сюда_твой_приватный_ключ
DREAMDEX_REST_URL=https://api.dreamdex.io/v0
DREAMDEX_CHAIN_RPC=https://api.infra.mainnet.somnia.network/
DASHBOARD_PORT=3000
```
Сохрани: **Ctrl+O → Enter → Ctrl+X**.

## 7. Запустить под pm2 (живёт 24/7, сам перезапускается)
```
npm install -g pm2
pm2 start ecosystem.config.cjs
pm2 save
pm2 startup
```
(последняя команда выведет ещё одну строку — скопируй её и тоже выполни, это включает автозапуск после перезагрузки сервера).

Проверка, что работает:
```
pm2 status
pm2 logs dreamdex
```
В логах должны пойти строки `equity=... gas=... актив=growth` и сделки.

## 8. Смотреть дашборд (безопасно, без открытия порта)
На своём ПК в PowerShell:
```
ssh -L 3000:localhost:3000 root@ТВОЙ_IP
```
Не закрывая это окно, открой в браузере **http://localhost:3000** — увидишь дашборд с сервера.

## 9. Базовая защита сервера
```
ufw allow 22
ufw enable
```
(порт 3000 НЕ открываем — дашборд только через туннель). По возможности смени root-пароль на длинный или настрой вход по SSH-ключу.

---

## Шпаргалка pm2
| Команда | Что делает |
|---|---|
| `pm2 status` | статус бота |
| `pm2 logs dreamdex` | живой лог |
| `pm2 restart dreamdex` | перезапустить (после правок кода/конфига) |
| `pm2 stop dreamdex` | остановить (снимет ордера на следующем тике не сделает — останови аккуратно) |
| `pm2 delete dreamdex` | убрать из pm2 |

## Обновить код/конфиг на сервере
1. Залей изменённые файлы через WinSCP (поверх старых).
2. `pm2 restart dreamdex`

## Гигиена газа
50 SOMI хватает надолго (≈0.002 SOMI/tx). Поглядывай на карточку «Газ» в дашборде; если упадёт к ~5 SOMI — доложи SOMI на зачётный кошелёк.
